package org.usfirst.frc.team5980.robot.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class FmsLlAutoCommandPosition2 extends CommandGroup {

    public FmsLlAutoCommandPosition2() {
      
    	/* Put Cube into Switch to the left, grab another cube
    
    	//addSequential(new DriveForwardAutoCommand(power, distance, heading));
    	//addSequential(new RotateToHeadingCommand(degrees, power));
    	//addSequential(new DriveForwardAutoCommand(p, d, h));
    	//addSequential(new RotateToHeadingCommand(d, p));
    	//addSequential(new DriveForwardAutoCommand(p, d, h));
    	//addParallel(new ElevatorLiftCommand(lift for time));
    	//addSequential(new DropCubeCommand(power)); 
    	 * 
    	//addSequential(new ElevatorDropCommand(drop for time));
    	//addSequential(new DriveBackwardsAutoCommand(p, d, h));
    	//addParallel(new RotateToHeading(d, p));
    	//addSequential(new DriveForwardAutoCommand(p, d, h));
    	//addParallel(new GrabCubeCommand(power));
    	     	 */
    }
    
}


    
    