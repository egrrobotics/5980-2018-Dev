package org.usfirst.frc.team5980.robot.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class FmsRlAutoCommandPosition1 extends CommandGroup {

    public FmsRlAutoCommandPosition1() {
        /* Place cube on the left side switch
         * 
         addSequential(new DriveForwardAuto(power, distance, heading));
         addParallel(new ElevatorLiftCommand(lift for time));
         addParallel(new TurretCommand(spin));
         addSequential(new PauseAuto(time));
         addSequential(new DropCubeCommand(power));
         */
    }
}
